# YaMDb API

## Описание проекта

YaMDb — это бэкенд-приложение для сбора отзывов пользователей на различные произведения: книги, фильмы, музыку.

Произведения делятся на категории и жанры. Пользователи могут оставлять текстовые отзывы и выставлять оценки от 1 до 10. На основе оценок автоматически рассчитывается средний рейтинг произведения. К отзывам можно добавлять комментарии.

Проект построен на Django REST Framework с использованием JWT-аутентификации и ролевой модели доступа.

---

## Функциональность

- Регистрация пользователей с подтверждением по email.
- JWT-аутентификация.
- Ролевая модель: user, moderator, admin.
- CRUD для категорий, жанров и произведений.
- Отзывы и оценки (1–10).
- Комментарии к отзывам.
- Рейтинг произведений (средняя оценка).
- Фильтрация, поиск и пагинация.
- Документация API (ReDoc).

---

## Стек технологий

| Компонент | Технология |
|-----------|------------|
| Бэкенд | Django 3.2+ |
| API | Django REST Framework 3.12+ |
| Аутентификация | JWT (SimpleJWT) |
| База данных | SQLite (разработка) |
| Документация | ReDoc |
| Фильтрация | Django Filter |

---

## Развёртывание проекта

Клонирование репозитория:

```bash
git clone https://github.com/Kirullk/api-yamdb.git
cd api-yamdb
```

Создание и активация виртуального окружения:

```bash
python3 -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
```

Установка зависимостей:

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

Выполнение миграций:

```bash
python manage.py migrate
```

Запуск сервера:

```bash
python manage.py runserver
```

Документация доступна по адресу:

```
http://127.0.0.1:8000/redoc/
```

---

## Пример запроса и ответа

### Регистрация пользователя

Запрос:

```http
POST /api/v1/auth/signup/
Content-Type: application/json

{
    "username": "kirill",
    "email": "kirill@example.com"
}
```

Ответ:

```json
{
    "email": "kirill@example.com",
    "username": "kirill"
}
```

### Получение JWT-токена

Запрос:

```http
POST /api/v1/auth/token/
Content-Type: application/json

{
    "username": "kirill",
    "confirmation_code": "123456"
}
```

Ответ:

```json
{
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
}
```

### Добавление отзыва

Запрос:

```http
POST /api/v1/titles/1/reviews/
Authorization: Bearer <token>
Content-Type: application/json

{
    "text": "Отличный фильм!",
    "score": 9
}
```

Ответ:

```json
{
    "id": 1,
    "text": "Отличный фильм!",
    "author": "kirill",
    "score": 9,
    "pub_date": "2025-04-07T12:00:00Z"
}
```

---

## Документация

Полная документация API доступна в формате ReDoc:

```
http://127.0.0.1:8000/redoc/
```

---

## Авторы проекта

| Роль | Имя | Что делал |
|------|-----|-----------|
| Первый разработчик | Кирилл Шишлов | Модель пользователя, система регистрации и аутентификации, права доступа, JWT-токены, подтверждение через email, профиль пользователя, управление пользователями |
| Второй разработчик | Вячеслав Пак | Модели, вьюсеты и эндпоинты для произведений, категорий и жанров, импорт данных из CSV |
| Третий разработчик | Роман Калининченко | Модели, вьюсеты и эндпоинты для отзывов, комментариев и рейтинга произведений |

---

## Контакты

| Имя | Telegram |
|-----|----------|
| Кирилл Шишлов | @kiyrer |
| Вячеслав Пак | @silentway2 |
| Роман Калининченко | @sqwhh |